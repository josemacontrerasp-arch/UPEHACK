# Copia este archivo como .env solo para desarrollo local con `vercel dev`.
# En producción, esta variable se configura en:
#   Vercel → Project → Settings → Environment Variables
# NUNCA pongas la key real en este archivo ni la subas al repo.

OPENAI_API_KEY=sk-tu-key-aqui
